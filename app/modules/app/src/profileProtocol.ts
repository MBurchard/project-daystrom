// Keep these values aligned through scripts/profile-protocol.spec.ts.

/** Profile placeholder used while importing the first detected account. */
export const INITIAL_PROFILE_STEM = 'initial';

/** Profile placeholder used while creating a new local account profile. */
export const NEW_ACCOUNT_PROFILE_STEM = 'new_account';
